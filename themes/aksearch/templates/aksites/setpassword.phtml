<?
    // Set up page title:
    $this->headTitle($this->translate('setPassword'));

    // Set up breadcrumbs:
    $this->layout()->breadcrumbs = '<li><a href="' . $this->url('myresearch-home') . '">' . $this->transEsc('Your Account') . '</a></li>' . '<li class="active">' . $this->transEsc('setPassword') . '</li>';
?>

<h2><?=$this->transEsc('setPassword') ?></h2>
<?=$this->flashmessages() ?>

<form id=setPassword class="form-horizontal" action="<?=$this->url('aksites-setpassword') ?>" method="post" data-toggle="validator" role="form">    
    <div>
    	<?=$this->auth()->getSetPasswordForm() ?>
    </div>
    
    <div>
	    <table class="table">
	    	<tr>
	    		<td>
	    			<a href="<?= $this->url('home') ?>" id="cancel" name="cancel" class="btn btn-default"><?=$this->transEsc('Cancel')?></a>
	    		</td>
	    		<td align="right">
	    			<input class="btn btn-primary" name="submit" type="submit" value="<?=$this->transEsc('Submit')?>" />
	    		</td>
	    	</tr>
	    </table>
    </div>
  </form>